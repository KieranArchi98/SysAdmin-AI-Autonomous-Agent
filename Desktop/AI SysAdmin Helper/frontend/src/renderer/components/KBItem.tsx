
export const KBItem = ({ title }: any) => (
    <div className="py-2 border-b border-gray-50 last:border-0">
        <p className="text-sm font-medium text-zinc-800 hover:text-blue-600 cursor-pointer">{title}</p>
    </div>
);
